package com.akash.test;

public interface Sample {
	
//	int id; // its because only final variable so they need to be initialized.
	int emoloyee= 100; // can create the object of interface
	// its legal for an interface to extend multiple interface
	
	void work();
	void enjoy();

}
